{% extends 'AppBundle:default:base.html.twig' %}

{% block body %}
    <h1>Image</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Fichiersource</th>
                <td>{{ image.fichierSource }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ image.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('business_image_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('business_image_edit', { 'id': image.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
