{% extends 'AppBundle:default:base.html.twig' %}

{% block body %}
    <h1>Villes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Pays</th>
                <th>Details</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ville in villes %}
            <tr>
                <td><a href="{{ path('business_ville_show', { 'id': ville.id }) }}">{{ ville.id }}</a></td>
                <td>{{ ville.libelle }}</td>
                <td>{{ ville.pays }}</td>
                <td>{{ ville.details }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('business_ville_show', { 'id': ville.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('business_ville_edit', { 'id': ville.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('business_ville_new') }}">Create a new ville</a>
        </li>
    </ul>
{% endblock %}
